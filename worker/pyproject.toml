[project]
name = "yt-transcript-worker"
version = "1.0.0"
description = "Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "youtube-transcript-api>=1.2.0",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]